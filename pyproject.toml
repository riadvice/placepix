[project]
name = "placepix"
version = "1.0.0"
description = "A self-hosted placeholder image server inspired by lorem-server and picsum.photos"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.141.1",
    "uvicorn[standard]>=0.52.4",
    "jinja2>=3.1.6",
    "python-multipart>=0.0.32",
    "pillow>=12.3.0",
    "pydantic-settings>=2.15.0",
    "python-dotenv>=1.2.3",
    "watchdog>=6.0.0",
    "pyyaml>=6.0.3",
    "pillow-avif-plugin>=1.6.0",
    "opencv-contrib-python-headless>=5.0.0.93",
    "numpy>=2.5.2",
    "boto3>=1.43.80",
    "apscheduler>=3.11.3",
    "requests>=2.34.2",
    "blurhash>=1.1.5",
    "markdown>=3.10.3",
    "pygments>=2.21.0",
    "multiavatar>=1.0.3",
]

[project.optional-dependencies]
dev = [
    "pytest>=9.1.1",
    "pytest-cov>=7.1.0",
    "pytest-xdist>=3.8.0",
    "httpx>=0.28.1",
    "ruff>=0.16.4",
]

[project.scripts]
placepix = "src.main:run"
placepix-stats = "src.admin:main"

[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors (FLK-E501, FLK-E302, FLK-E303, FLK-E305, FLK-E402, FLK-W293)
    "W",   # pycodestyle warnings
    "I",   # isort (import sorting - PYL-C0412)
    "F",   # Pyflakes
]
ignore = []

[tool.ruff.lint.pycodestyle]
max-doc-length = 100

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
skip-magic-trailing-comma = false
line-ending = "lf"

[tool.ruff.lint.isort]
combine-as-imports = true
force-sort-within-sections = true
known-first-party = ["src"]

[tool.coverage.run]
concurrency = ["thread", "multiprocessing"]
source = ["src"]
branch = true

[tool.coverage.report]
fail_under = 90

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
addopts = "-q -m 'not slow'"
markers = [
    "slow: marks tests as slow (deselect with '-m \"not slow\"')"
]
